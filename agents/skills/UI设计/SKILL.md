# UI/UX Pro Max

提供专业级 UI/UX 设计智能，支持多平台界面设计、设计系统生成与实现规范。

## 使用方法

使用 `@UI设计` 或 `@UI/UX` 触发此技能。

## Prerequisites

检查 Python 是否已安装：

```bash
python3 --version || python --version
```

---

## 核心工作流程

当用户请求 UI/UX 工作（设计、构建、创建、实现、审查、修复、改进）时，遵循以下流程：

### Step 1: 分析用户需求

从用户请求中提取关键信息：
- **产品类型**: SaaS、电商、作品集、仪表盘、落地页等
- **风格关键词**: 极简、活泼、专业、优雅、暗黑模式等
- **行业**: 医疗、金融科技、游戏、教育等
- **技术栈**: React、Vue、Next.js，或默认 `html-tailwind`

### Step 2: 生成设计系统 (必需)

**始终使用 `--design-system` 开始** 获取完整的设计建议：

```bash
python3 ~/Desktop/wj/multi-agent-orchestration/agents/skills/UI设计/scripts/search.py "<产品类型> <行业> <关键词>" --design-system [-p "项目名称"]
```

此命令会：
1. 并行搜索 5 个领域（产品、风格、颜色、落地页、排版）
2. 应用推理规则选择最佳匹配
3. 返回完整设计系统：模式、风格、颜色、排版、效果
4. 包含需避免的反模式

**示例：**
```bash
python3 ~/Desktop/wj/multi-agent-orchestration/agents/skills/UI设计/scripts/search.py "fintech trading dashboard dark" --design-system -p "金融"
```

### Step 3: 补充详细搜索（按需）

获取设计系统后，使用领域搜索获取更多细节：

| 需求 | 领域 | 示例 |
|------|------|------|
| 更多风格选项 | `style` | `--domain style "glassmorphism dark"` |
| 图表建议 | `chart` | `--domain chart "real-time dashboard"` |
| UX 最佳实践 | `ux` | `--domain ux "animation accessibility"` |
| 替代字体 | `typography` | `--domain typography "elegant luxury"` |
| 落地页结构 | `landing` | `--domain landing "hero social-proof"` |

**示例：**
```bash
python3 ~/Desktop/wj/multi-agent-orchestration/agents/skills/UI设计/scripts/search.py "glassmorphism dark" --domain style
```

### Step 4: 技术栈指南

获取特定技术栈的实现最佳实践。如果用户未指定，**默认使用 `html-tailwind`**。

```bash
python3 ~/Desktop/wj/multi-agent-orchestration/agents/skills/UI设计/scripts/search.py "<关键词>" --stack html-tailwind
```

支持的技术栈：`html-tailwind`、`react`、`nextjs`、`vue`、`svelte`、`swiftui`、`react-native`、`flutter`、`shadcn`、`jetpack-compose`

---

## 持久化设计系统（Master + Overrides 模式）

保存设计系统以便跨会话使用：

```bash
python3 ~/Desktop/wj/multi-agent-orchestration/agents/skills/UI设计/scripts/search.py "<查询>" --design-system --persist -p "项目名称"
```

创建：
- `design-system/MASTER.md` — 全局设计规则源
- `design-system/pages/` — 页面特定覆盖

**页面特定覆盖：**
```bash
python3 ~/Desktop/wj/multi-agent-orchestration/agents/skills/UI设计/scripts/search.py "<查询>" --design-system --persist -p "项目名称" --page "dashboard"
```

---

## 专业 UI 通用规则

### 图标与视觉元素

| 规则 | ✅ 正确 | ❌ 错误 |
|------|--------|--------|
| **禁用 emoji 图标** | 使用 SVG 图标 (Heroicons, Lucide) | 使用 🎨 🚀 ⚙️ 作为 UI 图标 |
| **稳定悬停状态** | 使用颜色/透明度过渡 | 使用 scale 变换导致布局偏移 |
| **正确品牌 Logo** | 从 Simple Icons 获取官方 SVG | 猜测或使用错误 Logo |

### 交互与光标

| 规则 | ✅ 正确 | ❌ 错误 |
|------|--------|--------|
| **光标指针** | 所有可点击元素添加 `cursor-pointer` | 交互元素保持默认光标 |
| **悬停反馈** | 提供视觉反馈（颜色、阴影、边框） | 无交互指示 |
| **平滑过渡** | 使用 `transition-colors duration-200` | 瞬间状态变化或太慢 (>500ms) |

### 明暗模式对比度

| 规则 | ✅ 正确 | ❌ 错误 |
|------|--------|--------|
| **亮色玻璃卡片** | 使用 `bg-white/80` 或更高 | 使用 `bg-white/10`（太透明） |
| **亮色文字对比** | 使用 `#0F172A` (slate-900) | 使用 `#94A3B8` (slate-400) |
| **边框可见性** | 亮色模式使用 `border-gray-200` | 使用 `border-white/10`（不可见） |

---

## 交付前检查清单

### 视觉质量
- [ ] 无 emoji 作为图标（使用 SVG）
- [ ] 所有图标来自一致图标集
- [ ] 悬停状态不导致布局偏移
- [ ] 直接使用主题颜色 (bg-primary) 而非 var()

### 交互
- [ ] 所有可点击元素有 `cursor-pointer`
- [ ] 悬停状态提供清晰视觉反馈
- [ ] 过渡平滑 (150-300ms)
- [ ] 焦点状态对键盘导航可见

### 明暗模式
- [ ] 亮色模式文字对比度足够 (4.5:1 最小)
- [ ] 玻璃/透明元素在亮色模式可见
- [ ] 边框在两种模式都可见

### 布局
- [ ] 浮动元素与边缘有适当间距
- [ ] 无内容隐藏在固定导航栏后
- [ ] 在 375px、768px、1024px、1440px 响应式
- [ ] 移动端无水平滚动

### 无障碍
- [ ] 所有图片有 alt 文本
- [ ] 表单输入有标签
- [ ] 颜色不是唯一指示器
- [ ] 尊重 `prefers-reduced-motion`
