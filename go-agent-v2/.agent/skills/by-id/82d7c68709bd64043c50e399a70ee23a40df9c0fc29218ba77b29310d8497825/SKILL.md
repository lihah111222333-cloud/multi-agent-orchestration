---
name: 验收官规范
description: 专业的交付验收指南，涵盖用户验收测试(UAT)、完成定义(DoD)、质量关卡(Quality Gates)与发布签发流程。
tags: [uat, qa, acceptance, definition-of-done, quality-assurance, release, testing, 验收, 质量保证, 发布标准]
---

# 验收官规范 (Acceptance Officer Standards)

本指南定义了 wjboot 项目的交付验收标准。验收官（Acceptance Officer）是产品质量的守门人，拥有对功能上线的一票否决权。

## 何时使用

在以下场景参考此规范：
*   **功能开发完成**：工程师提交 PR 或声称"开发完成"时。
*   **Sprint 结束**：准备发布新版本前。
*   **线上发布**：执行灰度或全量发布决策时。
*   **Bug 验证**：确认 Bug 是否被彻底修复且无副作用时。

---

## 第一部分：验收核心思维 (The Mindset)

### 1. 疑罪从有 (Guilty Until Proven Innocent)
*   默认假设软件是有 Bug 的，直到经过严格测试证明它是可用的。
*   不要轻信开发者的"我本地测过了"。验收必须在**类生产环境 (Staging/Prod)** 进行。

### 2. 用户视角 (User Perspective)
*   抛开技术实现细节，像小白用户一样思考和操作。
*   不仅验证"功能可用"，还要验证"易用性"和"体验流畅度"。

### 3. happy path 之外
*   80% 的 Bug 藏在 20% 的异常流程中。重点测试断网、超时、非法输入、并发操作等边界情况。

---

## 第二部分：完成定义 (Definition of Done - DoD)

一个 Story 或 Task 只有满足以下所有条件，才能标记为 `Done`：

### 1. 代码质量
*   [ ] 代码已合并入主分支 (main/develop)。
*   [ ] CI 流水线 (Build, Lint, Unit Test) 全部绿色通过。
*   [ ] 静态代码分析无 Critical/High 级别警告。

### 2. 功能完整
*   [ ] 满足 PRD 中定义的所有 AC (Acceptance Criteria)。
*   [ ] UI/UX 还原度达到设计稿的 95% 以上。
*   [ ] 埋点数据已正确上报。

### 3. 测试覆盖
*   [ ] 核心逻辑有单元测试覆盖。
*   [ ] 关键链路通过相关 E2E 测试。
*   [ ] **无回退 (No Regression)**：通过冒烟测试，确保旧功能未受影响。

### 4. 文档完备
*   [ ] API 文档已更新 (Swagger/Postman)。
*   [ ] 用户手册或 Release Note 已起草。
*   [ ] 数据库变更脚本已测试并归档。

---

## 第三部分：验收流程 (The Acceptance Process)

### 1. 桌面检查 (Desk Check)
*   **时机**：代码合并前。
*   **动作**：开发者在本地向验收官/PM 演示功能。
*   **目的**：快速发现明显问题，避免无效的集成测试构建。

### 2. 预发布验收 (Staging UAT)
*   **时机**：部署到 Staging 环境后。
*   **流程**：
    1.  **冒烟测试**：验证登录、主流程是否通畅。如果不通，直接打回 (Reject)。
    2.  **探索性测试**：不按脚本，随机操作，模拟用户真实行为。
    3.  **异常测试**：断网重连、弱网加载、杀死进程重启。
    4.  **数据验证**：检查数据库落库是否准确，日志是否有报错。

### 3. 生产环境验证 (Production Verification)
*   **时机**：代码上线后立即执行。
*   **动作**：使用测试账号进行全流程验证。
*   **注意**：严禁污染真实用户数据。测试产生的脏数据需及时清理或标记。

---

## 第四部分：Bug 严重性分级 (Severity Classification)

验收中发现的问题必须按严重性分级，以决定发布策略。

| 等级 | 定义 | 发布决策 |
|------|------|----------|
| **Blocker (P0)** | 系统崩溃、数据丢失、核心流程阻塞、安全漏洞 | **立即终止发布 (Stop Ship)** |
| **Critical (P1)** | 主要功能有缺陷但有规避方案、严重 UI 错位 | **修复后发布** (除非管理层特批) |
| **Major (P2)** | 次要功能缺陷、性能略有下降、文案错误影响理解 | 记录 Ticket，可带病上线，下个版本修复 |
| **Minor (P3)** | 轻微 UI 瑕疵、文案拼写错误、建议性优化 | 进入 Backlog，排期优化 |

---

## 第五部分：发布签发 (Release Sign-off)

发布前的最终确认仪式。

> **✅ Release Sign-off Checklist**
>
> **版本号**: v1.2.0
> **验收官**: @QA_Lead
>
> - [ ] 所有 P0/P1 Bug 已清零。
> - [ ] 遗留的 P2 问题已有明确的修复计划和风险评估。
> - [ ] 性能测试通过（响应时间/吞吐量达标）。
> - [ ] 回滚方案 (Rollback Plan) 已验证可用。
> - [ ] 客服/运营团队已获知新版本变更点。
>
> **结论**: [ GO / NO-GO ]

---

## 结语

验收官是质量的最后一道防线。请记住：**漏给用户的每一个 Bug，都是对品牌信誉的一次透支。**

---

## 第六部分：自动化验收脚本

### Playwright 验收测试模板

```python
from playwright.sync_api import sync_playwright
import json
from datetime import datetime

def run_acceptance_tests():
    """执行验收测试套件"""
    results = {
        "version": "v1.2.0",
        "timestamp": datetime.now().isoformat(),
        "tests": [],
        "passed": 0,
        "failed": 0
    }
    
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True)
        page = browser.new_page()
        
        # 测试 1：首页加载
        test_result = test_homepage_load(page)
        results["tests"].append(test_result)
        
        # 测试 2：登录流程
        test_result = test_login_flow(page)
        results["tests"].append(test_result)
        
        # 测试 3：核心功能
        test_result = test_core_feature(page)
        results["tests"].append(test_result)
        
        browser.close()
    
    # 统计结果
    results["passed"] = sum(1 for t in results["tests"] if t["status"] == "PASS")
    results["failed"] = sum(1 for t in results["tests"] if t["status"] == "FAIL")
    
    # 输出报告
    print(json.dumps(results, indent=2, ensure_ascii=False))
    return results["failed"] == 0

def test_homepage_load(page):
    """验收：首页 3 秒内加载完成"""
    try:
        start = datetime.now()
        page.goto("http://localhost:3000")
        page.wait_for_load_state("networkidle")
        duration = (datetime.now() - start).total_seconds()
        
        if duration < 3:
            return {"name": "首页加载", "status": "PASS", "duration": f"{duration:.2f}s"}
        else:
            return {"name": "首页加载", "status": "FAIL", "error": f"加载时间 {duration:.2f}s 超过 3s"}
    except Exception as e:
        return {"name": "首页加载", "status": "FAIL", "error": str(e)}

def test_login_flow(page):
    """验收：登录流程正常"""
    try:
        page.goto("http://localhost:3000/login")
        page.fill('input[name="email"]', 'test@example.com')
        page.fill('input[name="password"]', 'password123')
        page.click('button[type="submit"]')
        page.wait_for_url("**/dashboard", timeout=5000)
        
        return {"name": "登录流程", "status": "PASS"}
    except Exception as e:
        page.screenshot(path="/tmp/login_fail.png")
        return {"name": "登录流程", "status": "FAIL", "error": str(e)}

if __name__ == "__main__":
    success = run_acceptance_tests()
    exit(0 if success else 1)
```

### Shell 验收脚本

```bash
#!/bin/bash
# acceptance_check.sh - 自动化验收检查

set -e

echo "🔍 开始验收检查..."

# 1. 构建检查
echo "1️⃣ 检查构建状态..."
if ! make build; then
    echo "❌ 构建失败"
    exit 1
fi
echo "✅ 构建成功"

# 2. 单元测试
echo "2️⃣ 运行单元测试..."
if ! go test ./... -cover | tee test_results.txt; then
    echo "❌ 单元测试失败"
    exit 1
fi
echo "✅ 单元测试通过"

# 3. 覆盖率检查
COVERAGE=$(grep "coverage:" test_results.txt | awk '{print $2}' | tr -d '%')
if (( $(echo "$COVERAGE < 80" | bc -l) )); then
    echo "❌ 测试覆盖率 ${COVERAGE}% 低于 80%"
    exit 1
fi
echo "✅ 测试覆盖率 ${COVERAGE}%"

# 4. 静态分析
echo "3️⃣ 运行静态分析..."
if ! golangci-lint run; then
    echo "❌ 静态分析发现问题"
    exit 1
fi
echo "✅ 静态分析通过"

# 5. 安全扫描
echo "4️⃣ 运行安全扫描..."
if ! gosec ./...; then
    echo "⚠️ 发现安全警告（需人工评估）"
fi

echo ""
echo "🎉 验收检查完成！"
echo "   构建: ✅"
echo "   测试: ✅ (覆盖率 ${COVERAGE}%)"
echo "   静态分析: ✅"
echo ""
echo "可以进入下一阶段验收。"
```

---

## 第七部分：原子化提交规范验收

> **验收要求**：代码提交必须遵循原子化提交原则。

### 提交规范检查点

| 检查项 | 说明 |
|--------|------|
| **单一职责** | 每个提交只包含一个逻辑变更 |
| **可编译** | 每个提交后代码应能编译通过 |
| **可测试** | 每个提交后测试应能通过 |
| **信息清晰** | 遵循 Conventional Commits 格式 |

> 📚 **完整规范**：参考 [Git原子提交规范](../Git原子提交规范/SKILL.md)

---

## 审查清单

### 功能验收
- [ ] 所有用户故事的接受标准已满足
- [ ] 关键用户流程可正常完成
- [ ] 错误处理和边界条件已测试
- [ ] 兼容性测试通过（浏览器/设备）

### 性能验收
- [ ] 页面加载时间 < 3 秒
- [ ] API 响应时间 < 200ms (P95)
- [ ] 无明显内存泄漏
- [ ] 并发测试通过

### 安全验收
- [ ] 无 P0/P1 安全漏洞
- [ ] 敏感数据处理符合规范
- [ ] 权限控制正确

### 发布准备
- [ ] 回滚方案已验证
- [ ] 监控告警已配置
- [ ] 文档已更新
- [ ] 相关团队已通知



---

## ⚠️ 强制输出 Token 空间

> **重要规则**：使用此技能时，必须在每次重要输出前检查上下文空间。

### 输出规范

所有对话回复内容都要输出

### 输出格式

```
📊 剩余上下文空间: ~{百分比}%
```

### 告警与自动保存

**当剩余上下文空间 ≤ 30%（即已使用 ≥ 70%）时，必须执行：**

1. **立即暂停当前工作**
2. **保存工作进度**：创建 `.agent/workflows/checkpoint-{timestamp}.md`
3. **通知用户**：
   ```
   ⚠️ 上下文空间即将耗尽 (剩余 ~{百分比}%)
   📋 工作进度已保存至: .agent/workflows/checkpoint-{timestamp}.md
   请检查后决定是否继续或开启新对话
   ```
