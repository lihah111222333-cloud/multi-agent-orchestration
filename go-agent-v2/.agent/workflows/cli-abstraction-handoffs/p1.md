# P1 Handoff

## Scope
- phase: P1
- owner: Codex (test branch)
- start_at: 2026-02-24 00:58:07 +0800
- end_at: 2026-02-24 01:12:00 +0800

## Goal & Boundaries
- Build `internal/agentcore` abstraction layer for CLI-agnostic shared types/interfaces.
- Keep runner/apiserver migration out of P1.
- Modified paths:
  - `internal/agentcore/**`
  - `internal/codex/events.go`
  - `internal/codex/client.go`
  - `internal/codex/interface.go`

## Step 0 Baseline
- Initial blocker (`go build ./...`):
  - `cmd/agent-terminal/main.go:43:12: pattern frontend/dist/*: no matching files found`
- Minimal baseline recovery:
  - Created ignored placeholder asset: `cmd/agent-terminal/frontend/dist/index.html`
- Step 0 rerun status:
  - `go build ./...` ✅
  - `go test ./internal/codex/... -count=1` ✅
  - `go test ./internal/runner/... -count=1` ✅
  - `go test ./internal/apiserver/... -count=1` ✅
  - `go test ./internal/bus/... -count=1` ✅

## Changes
- Added `internal/agentcore/types.go`:
  - `Event`, `DynamicTool`, `DynamicToolCallData`
  - all generic `Event*` constants
  - generic DTOs: `TextData`, `ErrorData`, `WarningData`, `TokenCountData`, `SessionConfiguredData`, `ExecApprovalRequestData`, `ExecCommandBeginData`, `ExecCommandEndData`, `PatchApplyData`, `CollabAgentData`, `ThreadNameUpdatedData`, `TurnDiffData`
  - session DTOs: `ThreadInfo`, `ResumeThreadRequest`, `ForkThreadRequest`, `ForkThreadResponse`
- Added `internal/agentcore/client.go`:
  - `EventHandler`
  - `Client` (kept Phase-1 compatible signatures including `GetPort`, `SpawnAndConnect`, `SendDynamicToolResult(..., requestID *int64)`)
  - `ClientFactory func(port int, agentID string) Client`
- Added TDD contract test `internal/agentcore/phase1_contract_test.go`:
  - red: package missing
  - green: verifies event constants non-empty, wire tags, and cross-package compatibility assertions
- Updated `internal/codex/events.go`:
  - generic types/constants switched to `agentcore` aliases
  - session DTO switched to aliases
  - kept codex-only protocol types (`DynamicToolCallResponse`, `MCP*`, `Skill`, `SubmitMessage`, `CreateThread*`, `HealthResponse`, slash commands)
- Updated `internal/codex/client.go`:
  - `EventHandler` switched to alias: `type EventHandler = agentcore.EventHandler`
- Updated `internal/codex/interface.go`:
  - `type CodexClient = agentcore.Client`
  - `type ClientFactory = agentcore.ClientFactory`

## P1 Migration Mapping (codex -> agentcore -> alias retained)
- `codex.Event` -> `agentcore.Event` -> yes
- `codex.EventHandler` -> `agentcore.EventHandler` -> yes
- `codex.CodexClient` -> `agentcore.Client` -> yes
- `codex.ClientFactory` -> `agentcore.ClientFactory` -> yes
- `codex.DynamicTool` / `codex.DynamicToolCallData` -> `agentcore.*` -> yes
- `codex.ThreadInfo` / `ResumeThreadRequest` / `ForkThreadRequest` / `ForkThreadResponse` -> `agentcore.*` -> yes
- `codex` generic DTO + generic `Event*` constants -> `agentcore` -> yes

## Bus Compatibility Statement
- `internal/bus` 仍通过 `codex.Event` 路径访问事件类型；由于 `codex.Event` 现为 `agentcore.Event` alias，行为保持兼容。
- 本阶段未修改 `internal/bus/**`。

## Validation
- LSP:
  - Used `lsp_open_file` + `lsp_diagnostics` on all modified files.
  - Known limitation: gopls workspace root does not include `.worktrees/test`, so diagnostics report workspace-boundary warnings.
- Run-tool verification (`code_run`, as required):
  - `cd ../.worktrees/test/go-agent-v2 && go test ./internal/agentcore/... -count=1` ✅
  - `cd ../.worktrees/test/go-agent-v2 && go build ./internal/agentcore/...` ✅
  - `cd ../.worktrees/test/go-agent-v2 && go test ./internal/codex/... -count=1` ✅
  - `cd ../.worktrees/test/go-agent-v2 && go test ./internal/runner/... -count=1` ✅
  - `cd ../.worktrees/test/go-agent-v2 && go test ./internal/apiserver/... -count=1` ✅
  - `cd ../.worktrees/test/go-agent-v2 && go test ./internal/bus/... -count=1` ✅
  - `cd ../.worktrees/test/go-agent-v2 && go build ./...` ✅ (linker warnings only)

## Risks
- LSP semantic diagnostics are partially blocked by workspace boundary in this session.
- Step 0 baseline relied on local ignored placeholder dist asset; CI should use real frontend build artifact.

## Next
- next_phase: P2
- required_reading:
  - `.agent/workflows/cli-abstraction.md`
  - `.agent/workflows/cli-abstraction-handoffs/p1.md`
  - `.agent/workflows/cli-abstraction-handoffs/p1.checks.log`
  - `internal/agentcore/types.go`
  - `internal/agentcore/client.go`
  - `internal/codex/events.go`
  - `internal/codex/interface.go`
