# P2 Handoff

## Scope
- phase: P2
- owner: P2 Agent (test branch)
- base_commit: c1541c7
- start_at: 2026-02-24 01:12:xx +0800
- end_at: 2026-02-24 01:22:14 +0800

## Goal & Boundaries
- 目标: 仅完成 Phase 2，迁移 `internal/runner` 对通用类型的依赖到 `agentcore`。
- 遵守边界:
  - 修改: `internal/runner/**`
  - 未修改: `internal/apiserver/**`、`internal/bus/**`、`cmd/**`
  - `internal/codex/interface.go` 未触达
- 保持行为: app-server 失败后 REST fallback 路径与日志语义保持不变。

## Changes
- 修改 `internal/runner/manager.go`:
  - 通用类型迁移:
    - `codex.CodexClient` -> `agentcore.Client`
    - `codex.Event` -> `agentcore.Event`
    - `codex.DynamicTool` -> `agentcore.DynamicTool`
    - `codex.Event*` 通用常量 -> `agentcore.Event*`
  - 工厂类型迁移:
    - `appServerFactory/restFactory` 改为 `agentcore.ClientFactory`
  - 新增方法:
    - `SetClientFactories(appFactory, restFactory agentcore.ClientFactory)`
    - 线程安全更新；传入 `nil` 时保留现有工厂，避免破坏默认行为
  - 默认构造保留 codex 构造器:
    - `codex.NewAppServerClient`
    - `codex.NewClient`
- 新增 `internal/runner/manager_test.go` (TDD):
  - `TestSetClientFactoriesOverridesFactories`
  - 先红后绿验证 `SetClientFactories` 注入生效

## Runner Types Mapping (codex -> agentcore)
- `AgentProcess.Client`: `codex.CodexClient` -> `agentcore.Client`
- `AgentEvent.Event`: `codex.Event` -> `agentcore.Event`
- `EventHandler`: `func(... codex.Event)` -> `func(... agentcore.Event)`
- `AgentManager.appServerFactory/restFactory`: `clientFactory` -> `agentcore.ClientFactory`
- `Launch(... dynamicTools []codex.DynamicTool)` -> `Launch(... dynamicTools []agentcore.DynamicTool)`
- `handleEvent(... codex.Event)` -> `handleEvent(... agentcore.Event)`
- `SetOnOutput` 事件常量:
  - `codex.EventAgentMessageDelta` -> `agentcore.EventAgentMessageDelta`
  - `codex.EventExecCommandOutputDelta` -> `agentcore.EventExecCommandOutputDelta`
- fallback 合成事件:
  - `codex.Event{Type: codex.EventBackgroundEvent}` -> `agentcore.Event{Type: agentcore.EventBackgroundEvent}`

## SetClientFactories Signature & Call Sites
- 签名:
  - `func (m *AgentManager) SetClientFactories(appFactory, restFactory agentcore.ClientFactory)`
- 调用点:
  - 生产代码: 无（保持现状）
  - 测试: `internal/runner/manager_test.go`

## Fallback Behavior Notes
- 仍是: app-server `SpawnAndConnect` 失败 -> 记录 warn -> 尝试 REST 工厂 -> 成功则发送 degraded background event 并 warn。
- 失败路径仍保持:
  - 设置 `StateError`
  - 从 `agents` map 清理失败实例
  - 保留原有 error/warn 日志语义与决策字段。

## Validation
- 按要求执行并记录:
  - `rg -n 'codex\.' internal/runner/manager.go --glob '!**/*_test.go'`
  - `gofmt -w internal/runner/manager.go internal/runner/manager_test.go`
  - `rg -n 'func \(m \*AgentManager\) SetClientFactories\(' internal/runner/manager.go`
  - `rg -n 'agentcore\.ClientFactory' internal/runner/manager.go`
  - `go test ./internal/runner/... -count=1`
  - `go test ./internal/codex/... -count=1` (补充验证)
- LSP 合规:
  - 已对改动文件执行 `lsp_open_file` + `lsp_diagnostics`
  - 当前会话与 P1 一致，worktree 在 gopls workspace 外，诊断包含 workspace 告警；以 run-tool 编译/测试结果作为有效验证。

## Risks / Blockers
- 风险: gopls workspace 边界导致语义诊断受限（非代码风险）。
- 阻塞: none

## Next (for P3)
- 先读:
  - `.agent/workflows/cli-abstraction.md`
  - `.agent/workflows/cli-abstraction-handoffs/p2.md`
  - `.agent/workflows/cli-abstraction-handoffs/p2.checks.log`
- 建议先跑:
  - `rg -l 'codex\.' internal/apiserver/ --glob '!**/*_test.go' | sort`
- 明确不要做:
  - 不要触碰 `internal/bus/**`（白名单保留 codex event 依赖）
