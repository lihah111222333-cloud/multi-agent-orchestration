# P3 Handoff

## Scope
- phase: P3
- owner: P3 Agent (test branch)
- base_commit: a0ac525
- start_at: 2026-02-24 01:24:xx +0800
- end_at: 2026-02-24 01:38:01 +0800

## Goal & Boundaries
- 目标: 完成 Phase 3，迁移 `internal/apiserver/**` 的通用 `codex.*` 到 `agentcore.*`，并迁移 `cmd/app-server/main.go` 入口事件绑定。
- 修改边界:
  - 修改: `internal/apiserver/**`、`cmd/app-server/main.go`
  - 未修改: `internal/bus/**`、`internal/runner/**`
- 白名单保留:
  - `internal/apiserver/methods_thread.go` 仅保留 `codex.FindRolloutPath` / `codex.ReadRolloutMessagesWithTrim`

## Skill Execution
- `@后端`: 已按 Go 后端迁移规范执行（边界控制、类型迁移、gofmt、回归验证）。
- `@测试驱动开发`: 会话技能列表未发现独立技能文件；按你在提示中给出的 TDD 规则执行（先跑失败反查 RED，再迁移，再跑 GREEN 校验）。

## Changes
- Batch 1（工具与编排）迁移到 `agentcore`:
  - `internal/apiserver/server_dynamic_tools.go`
  - `internal/apiserver/server_dynamic_tools_ext_registry.go`
  - `internal/apiserver/server_dynamic_tools_actions_ext.go`
  - `internal/apiserver/server_dynamic_tools_hierarchy_ext.go`
  - `internal/apiserver/server_dynamic_tools_semantic_ext.go`
  - `internal/apiserver/server_dynamic_tools_xref_ext.go`
  - `internal/apiserver/orchestration_tools.go`
  - `internal/apiserver/resource_tools.go`
  - `internal/apiserver/code_run_tools.go`
  - `internal/apiserver/server_approval.go`
- Batch 2（生命周期与入口绑定）迁移到 `agentcore`:
  - `internal/apiserver/server_payload.go`
  - `internal/apiserver/methods_turn.go`
  - `internal/apiserver/methods_thread.go`（保留 rollout_reader 白名单 codex 调用）
  - `internal/apiserver/methods_helpers.go`
  - `cmd/app-server/main.go`

## High-Risk Points Verification
- `server_payload.go`:
  - `AgentEventHandler(agentID string) agentcore.EventHandler`
  - 事件参数改为 `agentcore.Event`
  - `EventStreamError` / `EventDynamicToolCall` 切换到 `agentcore` 常量
- `server_approval.go`:
  - `handleApprovalRequest(..., event agentcore.Event)`
- `server_dynamic_tools.go`:
  - `handleDynamicToolCall(..., event agentcore.Event)`
  - `DynamicToolCallData` 全链路切换到 `agentcore.DynamicToolCallData`
  - 动态工具数组全部切到 `[]agentcore.DynamicTool`
- `methods_turn.go`:
  - `resolveClientActiveTurnID(client agentcore.Client)`
  - `codex.DynamicTool` 参数位改为 `agentcore.DynamicTool`
  - `activeTurnIDReader` 上方补充并保留 `GetActiveTurnID()` 具体实现契约注释
- `methods_thread.go`:
  - `ResumeThreadRequest` / `ForkThreadRequest` 迁移到 `agentcore`
  - 通用事件常量改为 `agentcore.EventAgentMessage`
  - 仅保留 rollout_reader 白名单 codex 引用
- `cmd/app-server/main.go`:
  - `mgr.SetOnEvent(func(agentID string, event agentcore.Event) { ... })`

## Validation
- `go test ./internal/apiserver/... -count=1` ✅
- `go test ./internal/runner/... -count=1` ✅（联动回归）
- `go test ./cmd/app-server -count=1` ✅（入口编译链路）
- Step 3 alias-safe 反查（`verify-helpers.sh`）✅
- `apiserver` 通用 `codex.*` 残留反查（白名单外）✅

## LSP Compliance
- 改动前: 对全部目标文件执行 `lsp_open_file + lsp_diagnostics`。
- 迁移点引用追踪: 已执行 `lsp_references` 覆盖
  - `AgentEventHandler`
  - `resolveClientActiveTurnID`
  - `handleDynamicToolCall`
- 改动后: 对全部目标文件再次执行 `lsp_open_file + lsp_diagnostics`，无新增语义错误。
- gopls workspace 告警: 本阶段未出现该告警。

## Risks / Blockers
- blockers: none
- notes:
  - `methods_thread.go` 保留 `codex` 仅用于 rollout_reader 白名单函数；其余通用类型已迁移至 `agentcore`。

## Next
- next_phase: P4
- required_reading:
  - `.agent/workflows/cli-abstraction.md`
  - `.agent/workflows/cli-abstraction-handoffs/p3.md`
  - `.agent/workflows/cli-abstraction-handoffs/p3.checks.log`
  - `.agent/workflows/cli-abstraction-handoffs/p3.files.txt`
