# P5 Handoff

## Scope
- phase: P5
- owner: P5 Agent (test branch)
- start_at: 2026-02-24 02:04:01 +0800
- end_at: 2026-02-24 02:26:00 +0800
- base_commit: a990d7f
- objective: Step 5 cleanup + post-migration test rebuild + full gate regression

## Skill Execution
- active: `@后端`
- attempted: `@测试驱动开发`
- status: not found locally; recorded first in `p5.checks.log`, then executed fail-first -> minimal fix -> regression flow.

## Changes
- runner constructor decoupling:
  - `internal/runner/manager.go`
  - `NewAgentManager(...)` now returns `(*AgentManager, error)`.
  - nil factory arguments return explicit error.
  - removed production-path `internal/codex` import from `internal/runner/**` non-test code.
  - kept `SetClientFactories(appFactory, restFactory agentcore.ClientFactory)` as test-time injection point.
- composition root wiring:
  - `cmd/app-server/main.go` now uses constructor injection path and handles error:
    - `runner.NewAgentManager(codex.NewAppServerClient, codex.NewClient)`
- runner test rebuild:
  - `internal/runner/manager_test.go` rewritten with:
    - constructor nil-guard tests
    - factory override test
    - fallback regression test: `TestLaunchFallbackToRESTWhenAppServerSpawnFails`
- notifications constantization (layered):
  - `internal/apiserver/notifications.go`
  - replaced eligible string keys with `agentcore.Event*` constants.
  - apiserver-only keys kept as string and marked inline comment (`// apiserver-only, no agentcore constant`).
- agentcore event constants supplement:
  - `internal/agentcore/types.go`
  - added missing generic constants used by `notifications.go` mapping (turn aborted/plan, patch/file, reasoning compatibility, mcp compatibility, terminal interaction, approval request, etc.).
- codex whitelist retention note:
  - `internal/codex/events.go` adds explicit phase note: keep `codex.Event*` export path for `internal/bus` whitelist.
- compile-compat follow-up (minimal):
  - `cmd/agent-terminal/main.go` updated to match new constructor return contract to keep `go test ./...` green.

## Validation
- executed required Step 5 command block (full) with iterative fail->fix->rerun until green.
- required gates included and passed:
  - runner fallback gate
  - constructor signature + nil-guard gates
  - main constructor binding gate
  - runner/apiserver import whitelist gates
  - methods_thread codex whitelist gate
  - bus codex whitelist gate
- required global regression passed:
  - `go test ./... -count=1`
- notifications pre-analysis command executed before edits and repeated after edits; remaining no-constant keys are documented apiserver-only entries.
- LSP requirement executed pre/post edits on required paths (`internal/runner/**`, `internal/apiserver/**`, `internal/codex/**`, `cmd/app-server/main.go`, plus touched files), with diagnostics recorded in `p5.checks.log`.

## Risks / Notes
- no open blocker.
- LSP workspace diagnostics remain noisy for this `.worktrees/test/go-agent-v2` module context (known tooling scope issue); build/test gates are authoritative and all green.
- one minimal scope exception was required for compile compatibility: `cmd/agent-terminal/main.go` constructor callsite.

## Next
- next_phase: complete
- required_reading:
  - `.agent/workflows/cli-abstraction-handoffs/p5.checks.log`
  - `.agent/workflows/cli-abstraction-handoffs/p5.files.txt`
