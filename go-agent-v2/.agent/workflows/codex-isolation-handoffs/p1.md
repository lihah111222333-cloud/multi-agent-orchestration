# P1 阶段报告

## 阶段目标

在 `internal/apiserver` 包内完成 codex 符号聚合（不跨包），为 P3 子包落地降低冲突面；保持行为、签名与 JSON-RPC method name 不变。

## TDD 执行记录

1. 先新增结构性红灯测试 `TestP1CodexSymbolsAreIsolated`（`internal/apiserver/p1_codex_isolation_test.go`）。
2. 初始执行失败（RED）：`threadResumeTyped` 仍在 `methods_thread.go`。
3. 完成四组迁移后再次执行转绿（GREEN）。

## 变更摘要

1. `methods_turn.go` 拆出 codex 方法到 `methods_turn_codex.go`：
- `activeTurnIDReader`、`resolveClientActiveTurnID`
- `turnStartTyped`、`turnSteerTyped`、`turnInterrupt`、`turnForceComplete`
- `normalizeInterruptState`、`readThreadRuntimeState`、`waitInterrupt*`、`interruptSettleMode`
- `reviewStartTyped`

2. `methods_helpers.go` 拆出 codex 线程恢复/命令发送到 `methods_helpers_codex.go`：
- thread candidate/resume 族
- `ensureThreadReadyForTurn`、`registerBinding`
- slash command 解析与发送底层

3. `methods_thread.go` 拆出 codex 线程恢复/归档/rollout 到 `methods_thread_codex.go`：
- `threadResumeTyped`、`threadMessagesTyped`
- `threadArchiveTyped`、`threadUnarchiveTyped`、`threadNameSetTyped`、`threadRollbackTyped`
- rollout 与 archive 相关核心函数（含 manifest 读写）

4. `turn_tracker.go` 拆出 codex turn 跟踪与 stall 处理到 `turn_tracker_codex.go`：
- `trackedTurn` 结构
- begin/complete/finalize/peek 全链路
- stall 检测与自动 interrupt 全链路

## 验证结果

1. `go test ./internal/apiserver -run TestP1CodexSymbolsAreIsolated -count=1` ✅
2. `go test ./internal/apiserver/... -count=1` ✅
3. `go vet ./internal/apiserver/...` ✅
4. `go build ./...` ✅（仅存在既有 macOS linker warning，不阻塞）
5. `rg` 检查显示 `Client.Submit/SendCommand/GetThreadID/ResumeThread` 已主要聚合到 `*_codex.go`（`server_approval.go`、`server_payload.go` 仍有工作流允许的散点）。

## 风险与备注

1. 本阶段新增的 `p1_codex_isolation_test.go` 为结构约束测试，后续 P2/P3 可继续复用以防回退。
2. 未跨包迁移；仅在 `internal/apiserver` 内重排。
3. 未修改 JSON-RPC method 名称与参数结构。
