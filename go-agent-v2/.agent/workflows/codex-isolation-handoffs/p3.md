# P3 完成报告（复检补强）

## 阶段状态
- current_phase: P3
- status: done
- next_phase: P4

## 本轮补齐内容（针对复检缺口）
- `methods_thread_codex.go` 中以下重逻辑已迁入 `internal/apiserver/codexadapter/thread_archive.go`，原位置退化为薄委派：
  - `collectThreadArtifactCandidates`
  - `pruneArchivedCodexSourceFiles`
  - `restoreThreadArchiveSources`
  - `inspectThreadArchiveForRestore`
  - `findLatestThreadArchiveManifestPath`
  - `readThreadArchiveManifest`
  - `writeThreadArchiveManifest`
- `threadUnarchiveTyped` 改为通过 `s.codexAdapter` 调用归档恢复能力（参数装配 + 错误包装）。
- `turn_tracker_codex.go` 生命周期函数下沉到 `codexadapter/turn_tracker.go`，apiserver 仅做委派与依赖注入：
  - `hasActiveTrackedTurn`
  - `markTrackedTurnInterruptRequested`
  - `waitTrackedTurnTerminal`
  - `checkTurnStall`
  - `executeStallAutoInterrupt`
- `methods_helpers_codex.go` 中历史判定与候选解析重逻辑已下沉到 `internal/apiserver/codexadapter/thread_history.go`：
  - `threadExistsInHistory` → `codexadapter.ThreadExistsInHistory`
  - `resolveCodexThreadCandidates` → `codexadapter.ResolveCodexThreadCandidates`
  - apiserver 仅保留 store 回调装配（薄委派）

## Round 6 复核补齐（R1 / R3 / R4）
- R1（`ServerContext` 类型化）：
  - `codexadapter.ServerContext` 不再返回 `any`，改为显式类型：
    - `*runner.AgentManager`
    - `*uistate.PreferenceManager`
    - `*store.AgentCodexBindingStore`
    - `*store.AgentStatusStore`
    - `*uistate.RuntimeManager`
  - `internal/apiserver/server.go` 已补齐对应 typed accessor（含新增 `AgentStatusStore()`）。
- R3（commonadapter 扩容）：
  - 新增 `internal/apiserver/commonadapter/skills.go`，迁入通用纯函数（技能名规范化/匹配/LSP 工具引用提取等）。
  - `commonadapter` 由原 4 个函数扩展到 25 个函数，`methods_turn.go` 对应逻辑改为委派调用。
- R4（`*_codex*.go` 重逻辑继续下沉）：
  - 新增 `codexadapter` 文件：
    - `thread_messages.go`（rollout/source/pagination/hydration/stream）
    - `slash_command.go`（slash command 解析与执行）
    - `turn_tracker_helpers.go`（stall heartbeat/meta/hint/grace/touch）
    - `thread_manage.go`（`ThreadNameSet`/`ThreadUnarchive`）
  - `methods_thread_codex.go` 的 `threadUnarchiveTyped`、`threadNameSetTyped` 改为调用 `s.codexAdapter`。
  - `methods_turn_codex.go` 的 interrupt/force-complete 相关辅助逻辑改为调用 `codexadapter`。
  - `turn_tracker_codex.go` 的 stall 辅助链改为调用 `codexadapter` helper。
  - 删除遗留未使用声明，LSP 复审为 `no diagnostics`。

## 指标变化（本轮）
- `*_codex*.go` 总行数：`1565 -> 1086`（减少 479 行）。
- 当前统计：
  - `methods_helpers_codex.go`: 263 行
  - `methods_thread_codex.go`: 393 行
  - `methods_turn_codex.go`: 193 行
  - `turn_tracker_codex.go`: 171 行
- 说明：函数数仍较多（包装函数为主），但核心流程已进一步下沉到 `codexadapter`。

## 结构约束测试（TDD）
- RED：新增/强化结构测试后先失败：
  - `threadUnarchiveTyped` 未委派 `s.codexAdapter`
  - 归档 helper 仍在 apiserver 本地主逻辑
- GREEN：迁移后同组测试转绿。
- 关键测试文件：`internal/apiserver/methods_helpers_p3_test.go`
- 新增约束：`TestP3HistoryResolutionHelpersDelegateToCodexAdapter`，锁定历史判定/候选解析必须走 `codexadapter` 包委派。
- 新增行为测试：`internal/apiserver/codexadapter/thread_history_test.go`，覆盖历史判定与候选解析逻辑。

## 强制验证结果
- `go test ./internal/apiserver/... ./internal/apiserver/codexadapter/... ./internal/apiserver/commonadapter/... -count=1` 通过。
- `proc.Client.*` 白名单扫描通过（仅 `codexadapter/*` + `server_payload.go` + `server_approval.go`）。
- `go vet ./internal/apiserver/... ./internal/apiserver/codexadapter/... ./internal/apiserver/commonadapter/...` 通过。
- LSP 复审：`methods_helpers_codex.go`、`methods_thread_codex.go`、`codexadapter/thread_archive.go` 均为 `no diagnostics`。
- 明细见 `p3.checks.log`。
