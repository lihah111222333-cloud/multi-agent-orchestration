# P4 完成报告：全量验收与 CLI 适配准备度

## 阶段状态
- current_phase: P4
- status: done
- next_phase: P5

## 验收结论
已完成 P4 要求的全量验收与落盘。

1. `go build ./...` 通过（仅存在既有 macOS linker warning，不阻塞）。
2. `go test ./... -count=1` 通过。
3. `go vet ./...` 通过。
4. 结构扫描通过：
- P2：`apiserver` 不再保留动态 `lsp*` handler 实现，且 `lsp_*` 动态工具注册统一走 `s.lspTools.*`。
- P3：`proc.Client.*` 直接调用仅在 `internal/apiserver/codexadapter/*`。

## P4 验收输出

### 1) 文件清单与职责边界
- codexadapter: 见 `.agent/workflows/codex-isolation-handoffs/p4.files.txt` 中 `internal/apiserver/codexadapter/*`
  - 职责：Codex 运行时调用、线程归档/恢复、线程消息/历史、turn 跟踪与中断收敛。
- commonadapter: 见 `.agent/workflows/codex-isolation-handoffs/p4.files.txt` 中 `internal/apiserver/commonadapter/*`
  - 职责：通用输入/技能匹配/文本处理与无状态纯函数逻辑。
- internal/lsp: 见 `.agent/workflows/codex-isolation-handoffs/p4.files.txt` 中 `internal/lsp/*`
  - 职责：LSP manager + tool handlers（基础9 + 扩展10 + 辅助接口）统一承接。

### 2) apiserver 薄委派点位（样例）
- `internal/apiserver/methods_helpers_codex.go`
- `internal/apiserver/methods_thread_codex.go`
- `internal/apiserver/methods_turn_codex.go`
- `internal/apiserver/turn_tracker_codex.go`
- `internal/apiserver/server_payload.go`
- `internal/apiserver/server_approval.go`

### 3) 适配其他 CLI 最小接入面
- Codex: `internal/apiserver/codexadapter/adapter.go` + `ServerContext` typed capability。
- LSP: `internal/lsp/tool_handlers.go` 的 `NewToolHandlers` 构造与 `internal/apiserver/server_dynamic_tools*.go` 注册入口。
- Common: `internal/apiserver/commonadapter/common.go`、`internal/apiserver/commonadapter/skills.go`。

### 4) 状态推进
- 已更新 `LATEST.md` 为：`current_phase: P4`、`status: done`、`next_phase: P5`。

## 备注
- 本阶段以“验收闭环 + 交接落盘”为主，不新增业务行为。
- 后续进入 `P5/P6` 请切换到 `.agent/workflows/tool-consolidation.md`。
