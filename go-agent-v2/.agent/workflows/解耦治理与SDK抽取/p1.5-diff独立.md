---
description: P1.5 diff 逻辑独立 — 从 apiserver 抽为零耦合 internal/difftracker 包
---

# P1.5: diff 模块独立

> **⚡ 可并行** — 与 P1、P2 同时执行

## 前置条件

- [ ] P0 准备阶段已完成
- [ ] `internal/difftracker/emitter.go` 已创建

## 任务范围

### 需要创建的文件

- `internal/difftracker/tracker.go` — Tracker struct + BeginTracker + EmitDiffUpdate
- `internal/difftracker/git_ops.go` — runGit, gitRepoRootFromPath, listRepoDirtyPaths, collectPathLines, uniqueSortedPaths
- `internal/difftracker/unified_diff.go` — captureWorkingTreeFileSnapshots, readWorkingTreeFileSnapshot, readHeadFileSnapshot, buildIncrementalDiffText, buildUnifiedDiffBlock
- `internal/difftracker/args.go` — extractStringArg, extractBoolArg, normalizeDynamicToolName, shouldCaptureDynamicToolDiff

### 需要修改的文件

- `internal/apiserver/server_dynamic_tools_diff.go` — 缩减为 ~30 行薄 wrapper
- `internal/apiserver/server_dynamic_tools.go` — 更新 `beginDynamicToolDiffTracker` / `maybeEmitDynamicToolDiffUpdate` 调用签名和 import

### 禁止触碰的文件 ⚠️

- `internal/tools/*` (P1 负责)
- `internal/tooladapter/*` (P1 负责)
- `internal/apiserver/tool_providers.go` (P1 负责)
- `internal/lsp/*` (P2 负责)
- `internal/apiserver/codexadapter/*` (P3 负责)

## 执行步骤

// turbo-all

1. 创建 `internal/difftracker/git_ops.go`
   - 从 `server_dynamic_tools_diff.go` 搬移：`runGit`, `gitRepoRootFromPath`, `listRepoDirtyPaths`, `collectPathLines`, `uniqueSortedPaths`
   - 改 package 为 `difftracker`，导出函数名

2. 创建 `internal/difftracker/unified_diff.go`
   - 搬移：`fileContentSnapshot`, `captureWorkingTreeFileSnapshots`, `readWorkingTreeFileSnapshot`, `readHeadFileSnapshot`, `buildIncrementalDiffText`, `buildUnifiedDiffBlock`
   - 依赖 `go-difflib` 和 `pkg/logger`

3. 创建 `internal/difftracker/args.go`
   - 搬移：`extractStringArg`, `extractBoolArg`, `normalizeDynamicToolName`, `shouldCaptureDynamicToolDiff`

4. 创建 `internal/difftracker/tracker.go`
   - 实现 `Tracker` struct 和 `BeginTracker()`、`EmitDiffUpdate()`
   - 用 `WorkDirResolver` 回调替代 `*Server` 参数
   - 用 `DiffEmitter` 回调替代事件发射耦合

5. 修改 `server_dynamic_tools_diff.go` 为薄 wrapper
   ```go
   package apiserver

   import "github.com/multi-agent/go-agent-v2/internal/difftracker"

   func beginDynamicToolDiffTracker(s *Server, agentID, tool string, args map[string]any) difftracker.Tracker {
       return difftracker.BeginTracker(agentID, tool, args, func(id string) string {
           return getAgentWorkDirState(s, id)
       })
   }

   func maybeEmitDynamicToolDiffUpdate(s *Server, threadID, codexThreadID, tool string, tracker difftracker.Tracker) {
       tracker.EmitDiffUpdate(threadID, codexThreadID, tool, func(result difftracker.DiffResult) {
           // 构造 payload, emit uistate event, notify
       })
   }
   ```

6. 验证
   ```bash
   go build ./internal/difftracker/...
   go test ./internal/difftracker/...
   go build ./internal/apiserver/...
   go build ./...
   ```

7. 确认 difftracker 零 internal 依赖
   ```bash
   rg -n '"github.com/multi-agent/go-agent-v2/internal/' internal/difftracker/*.go
   # 应返回空
   ```

## 完成标准

- [ ] `internal/difftracker/` 包含 4 个新文件
- [ ] `difftracker` 仅依赖 `pkg/logger` + `go-difflib` + 标准库
- [ ] `server_dynamic_tools_diff.go` 缩减到 ~30 行
- [ ] `go build ./...` 通过
- [ ] 不修改其他 Agent 负责的文件

## 验证命令

```bash
cd /Users/mima0000/Desktop/wj/multi-agent-orchestration/go-agent-v2
go build ./internal/difftracker/...
go build ./internal/apiserver/...
go build ./...
```
